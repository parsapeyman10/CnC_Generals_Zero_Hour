<?xml version="1.0" encoding="utf-8"?>
<!-- AndroidManifest.xml - حفظ ساختار PC ولی به شکل APK -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ea.generals"
    android:versionCode="1"
    android:versionName="1.0-PCStructure">

    <!-- برای A30: حداقل اندروید 9 -->
    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="34" />

    <!-- قابلیت‌ها - A30 همه را دارد -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen.multitouch" android:required="true" />

    <!-- مجوزها - برای خواندن OBB و اینترنت (LAN بازی) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- A30 فقط Landscape مثل PC -->
    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_generals"
        android:label="Generals Zero Hour"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true">

        <!-- اکتیویتی اصلی - SDLActivity جایگزین WinMain Window -->
        <activity
            android:name="com.ea.generals.GeneralsActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:alwaysRetainTaskState="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- برای OBB Expansion File (فایل‌های .big) -->
            <meta-data android:name="android.app.lib_name" android:value="Generals" />
        </activity>

        <!-- Service برای دانلود OBB اگر از Play Store بیاید -->
        <service android:name="com.ea.generals.OBBDownloaderService" />

        <meta-data android:name="android.max_aspect" android:value="2.4" /> <!-- A30 19.5:9 -->
    </application>
</manifest>
